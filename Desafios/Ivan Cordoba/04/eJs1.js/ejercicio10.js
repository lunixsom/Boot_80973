const auto = {
    marca: 'Ford',
    motor: '1.6 nafta',
    color: 'rojo',
    año: '0km',
    velocidadMaxima: 180,
    modelo: 'Focus',
    cantidadDePuertas: 5,
};

document.write(`El auto más vendido en este año 2020 es el ${auto.marca} ${auto.modelo} que alcanza una velocidad máxima de ${auto.velocidadMaxima} km/h.`);